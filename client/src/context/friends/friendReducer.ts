import { Action, User, FriendStructure, AuthStatusPayload } from '../../models/';


const friendReducer = <T>(initialState: FriendStructure, action: Action<T>) => {
    return initialState;
}

export default friendReducer;